{
    "buildCommand": "cd .. && pnpm install --include=dev && pnpm run build && cd docs && pnpm run build",
    "installCommand": "cd .. && pnpm install --include=dev",
    "outputDirectory": ".next"
}